import XCTest
@testable import FloatingViewManager

final class FloatingViewManagerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(FloatingViewManager().text, "Hello, World!")
    }
}
